"use client";

import React from "react";

// Keep this component minimal. The actual server-side chat handler lives in
// `src/app/api/chat/route.ts`. This file is a lightweight client placeholder
// so any import paths that still reference ChatWidget don't break the build.
export default function ChatWidget() {
  return null;
}
